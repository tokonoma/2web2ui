import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import DateSelector from './DateSelector';

<Meta title="Form Controls|DateSelector" component={DateSelector}/>

# DateSelector

The `<DateSelector/>` allows users to use a calendar UI to select dates.

## Examples

### Default

<Preview>
  <DateSelector/>
</Preview>

## Usage

```js
import React from 'react';
import DateSelector from 'src/components/dateSelector/DateSelector';

const MyComponent = () => <DateSelector/>;
```
