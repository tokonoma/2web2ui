import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from './Button';
import { PageLink } from 'src/components/links'

<Meta title="Buttons|Button" component={Button} />

# Button

Use the `<Button/>` component for interactive elements that draw the user's attention.

## Props

<Props of={Button}/>

## Examples

### Default Variant

<Preview>
  <Button onClick={() => console.log('hello, world')}>Click Me</Button>
</Preview>

### Primary Variant

<Preview>
  <Button onClick={() => console.log('hello, world')} variant="primary">Click Me</Button>
</Preview>

### Secondary Variant

<Preview>
  <Button onClick={() => console.log('hello, world')} variant="secondary">Click Me</Button>
</Preview>

### Tertiary Variant

<Preview>
  <Button onClick={() => console.log('hello, world')} variant="tertiary">Click Me</Button>
</Preview>

### Destructive Variant

<Preview>
  <Button onClick={() => console.log('hello, world')} variant="destructive">Click Me</Button>
</Preview>

## Usage

### As an Interactive Element

For elements that trigger page behavior or submit forms.

```js
import { Button } from 'src/components/matchbox';

const MyComponent = () => {
  return (
    <Button variant="primary" onClick={() => console.log('hello, world')}>
      Click Me
    </Button>
  )
}
```

### As a Link

For elements that leverage the app's router or link to an external URL.

```js
import { PageLink } from 'src/components/links';
import { Button } from 'src/components/matchbox';

const MyComponent = () => {
  return (
    <PageLink as={Button} to="https://sparkpost.com">Go to SparkPost.com</PageLink>
  )
}
```

