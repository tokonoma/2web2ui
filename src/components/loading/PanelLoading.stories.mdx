import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Panel } from 'src/components/matchbox';
import PanelLoading from './PanelLoading';

<Meta title="Panels|Loading" component={PanelLoading} />

# Panel Loading

Use the `<PanelLoading/>` component as a placeholder for `<Panel/>` component. This is particularly
useful within `<Modal/>` components.

## Props

<Props of={PanelLoading} />

## Examples

### Default

<Preview>
  <Story name="default">
    <PanelLoading />
  </Story>
</Preview>

### With an Accent

<Story name="with an accent">
  <PanelLoading accent />
</Story>

### With a Shorter Min Height

<Story name="with a shorter min height">
  <PanelLoading minHeight="200px" />
</Story>

## Usage

```js
import React from 'react';
import { PanelLoading } from 'src/components/loading';
import { Panel } from 'src/components/matchbox';

const MyComponent = ({ loading }) => (loading ? <PanelLoading /> : <Panel />);
```
