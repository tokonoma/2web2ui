import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Loading } from './Loading';

<Meta title="Misc|Loading" component={Loading} />

# Loading

Use the `<Loading/>` component for loading states.

## Props

<Props of={Loading}/>

## Examples

### Default

<Preview>
  <Story name="default">
    <Loading/>
  </Story>
</Preview>

## Usage

```js
import React from 'react';
import { Loading } from 'src/components/loading';

const MyComponent = () => (
  <div>
    <Loading/>
  </div>
)
```

