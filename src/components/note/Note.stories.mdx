import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Note from './Note';

<Meta title="Misc|Note" component={Note}/>

# Note

The `<Note/>` is simply a styled container with a light background color.

## Examples

### Default

<Preview>
  <Note>With some children</Note>
</Preview>

## Usage

```js
import React from 'react';
import Note from 'src/components/note/Note';

const MyComponent = () => (
  <Note>With some children</Note>
)
```
