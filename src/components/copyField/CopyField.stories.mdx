import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CopyField from './CopyField';

<Meta title="Form Controls|CopyField" component={CopyField}/>

# CopyField

Use the `<CopyField/>` component to render a generic text input field with a copy button.

Note that this component needs to be paired with a valid HTML `<label/>` in order to [meet accessibility requirements](https://www.w3.org/WAI/tutorials/forms/labels/).

## Props

<Props of={CopyField}/>

## Examples

### Default

<Preview>
  <CopyField
    id="my-copy-field"
    value="My Copy Field Value"
    hideCopy={false}
    onChange={() => console.log(e.target.value)}
  />
</Preview>

## Usage

```js
import React, { useState } from 'react';
import { CopyField } from 'src/components';

const MyComponent = () => {
  const [fieldValue, setFieldValue] = useState('My Default Value');

  return (
    <CopyField
      id="my-copy-field"
      value={fieldValue}
      onChange={(e) => setFieldValue(e.target.value)}
    />
  )
}
```


